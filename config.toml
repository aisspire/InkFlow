# InkFlow 的第一版本地输入配置。
# 相对路径会按这个配置文件所在目录解析。
input_path = "note.md"

[review]
dir = "reviews"

[blog]
repo_path = ""
content_dir = "src/content/blog"

[publish]
build_command = "npm run build"
commit_message_template = "publish: {title}"
