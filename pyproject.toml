[project]
name = "inkflow"
version = "0.1.0"
description = "A LangGraph learning project for building a content workflow."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.0,<2.0",
]
